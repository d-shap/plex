#!/bin/bash
if [ "$1" != "" ];
then
    FILE_SOURCE="/plex/extensions/$1.bundle"
    FILE_TARGET="/plex/config/Plug-ins/$1.bundle"

    if [ ! -d "FILE_TARGET" ];
    then
        cp -r $FILE_SOURCE $FILE_TARGET
    fi
fi
